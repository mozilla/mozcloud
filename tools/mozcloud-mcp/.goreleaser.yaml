version: 2
project_name: mozcloud-mcp
dist: dist

before:
  hooks:
    - go mod tidy

builds:
  - id: mozcloud-mcp
    main: .
    binary: mozcloud-mcp
    env:
      - CGO_ENABLED=0
      - GOWORK=off
    goos:
      - darwin
      - linux
    goarch:
      - arm64
      - amd64
    ignore:
      - goos: darwin
        goarch: amd64
    ldflags:
      - -s -w
      - -X github.com/mozilla/mozcloud/tools/mozcloud-mcp/cmd.Version={{.Version}}

archives:
  - id: mozcloud-mcp
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ .Tag }}-{{ .ShortCommit }}-SNAPSHOT"

changelog:
  use: github
  sort: asc

blobs:
  - provider: gs
    bucket: "{{ .Env.RELEASE_BUCKET }}"
    directory: "mozcloud-mcp/{{ .Env.UPLOAD_DIR }}"

release:
  disable: true
